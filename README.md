To reproduce, run these commands and read the console output.

```sh
npm ci
npm start
```

The console shows this message, which should not be the case.

```sh
Generating a self signed certificate using openssl
```
